<%= render partial: 'shared/edit_collection_modal',
           locals: {
             collection: deck,
             frame_id: 'deck_modal',
             form_url: update_deck_deck_builder_path(deck),
             available_tags: local_assigns[:available_tags]
           } %>
