<div class='flex flex-col w-full min-w-0 gap-6 p-6 border rounded-xl border-highlight text-grey-text'>
  <div class='text-sm truncate'>
    <%= message %>
  </div>
  <% if count.present? %>
    <div class='text-4xl truncate'>
      <%= number_with_delimiter(count) %>
    </div>
  <% else %>
    <div class='text-4xl truncate'>
      <%= number_to_currency(value) %>
    </div>
  <% end %>
</div>
