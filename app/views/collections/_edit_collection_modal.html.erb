<%= render partial: 'shared/edit_collection_modal',
           locals: {
             collection: collection,
             frame_id: 'collection_modal',
             form_url: collection_path(collection),
             turbo_frame_target: '_top'
           } %>
