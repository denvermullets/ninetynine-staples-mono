<%#
  what the frame holds until its src lands. The app has no skeleton convention, so this follows the
  text-in-a-box one from commanders/index, wearing the analytics panel chrome
%>
<div class="p-12 text-center border rounded-3xl bg-foreground border-highlight">
  <p class="text-lg text-grey-text">Loading…</p>
</div>
